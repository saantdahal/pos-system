name: bhansa_ghar
description: "A premium restaurant management system."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
#publish_to: "" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  bloc: ^9.1.0 # for BLoC state management
  flutter_bloc: ^9.1.1 # for state management using BLoC pattern
  hydrated_bloc: ^10.1.1 # Persistent Bloc state
  equatable: ^2.0.7 # for value equality in Dart objects
  path_provider: ^2.1.5 # for accessing device file system paths
  hive: ^2.2.3 # lightweight and fast key-value database
  hive_flutter: ^1.1.0 # Hive support for Flutter
  go_router: ^17.0.0 # for declarative routing in Flutter
  flutter_screenutil: ^5.9.3 # for responsive UI design for android and ios devices
  flutter_launcher_icons: ^0.14.4 # for generating app launcher icons
  flutter_local_notifications: ^19.5.0 # for displaying local notifications
  timezone: ^0.10.1 # for timezone support in notifications
  json_annotation: ^4.9.0 # JSON serialization annotations
  qr_flutter: ^4.1.0 # for generating QR codes
  file_picker: ^10.3.7 # for picking files from the device
  shelf: ^1.4.1 # for web server
  shelf_router: ^1.1.4 # for routing in web server
  shelf_web_socket: ^2.0.0 # for web socket support in web server
  shelf_static: ^1.1.2 # for serving static files in web server
  network_info_plus: ^6.1.0 # for getting network information
  web_socket_channel: ^3.0.1 # for web socket support
  uuid: ^4.5.2 # for generating unique identifiers
  timeago: ^3.7.0 # for relative time formatting
  flutter_image_compress: ^2.4.0 # for compressing images
  share_plus: ^12.0.1 # for sharing content
  local_auth: ^3.0.0 # for biometric authentication
  crypto: ^3.0.7 # for cryptography operations
  flutter_background_service: ^5.1.0 # for background service
  flutter_tts: ^4.2.3 # for text to speech
  permission_handler: ^12.0.1 # for handling permissions
  http: ^1.2.0 # for making HTTP requests
  dio: ^5.4.0 # for advanced HTTP client
  google_sign_in: ^6.2.1 # for Google authentication
  pin_code_fields: ^8.0.1 # for OTP input fields
  flutter_dotenv: ^5.1.0 # for environment variables
  flutter_map: ^7.0.2 # for interactive maps
  latlong2: ^0.9.1 # for latitude and longitude coordinates
  geocoding: ^3.0.0 # for geocoding and reverse geocoding
  geolocator: ^13.0.1 # for getting current location
  image_picker: ^1.1.2 # for picking images from gallery or camera

  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2
  excel: ^4.0.3 # for Excel file import/export
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.0
  hive_generator: ^2.0.1
  json_serializable: ^6.8.0 # JSON serialization code generation
  mockito: ^5.4.4
  flutter_native_splash: ^2.4.1

flutter_native_splash:
  color: "#ffffff"
  image: "assets/icon/icon.png"
  android: true
  ios: true

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - .env
    - assets/web/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
